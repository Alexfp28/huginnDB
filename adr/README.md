# ADR / Gotchas index

This directory holds one Architecture Decision Record (ADR) per "hard-earned gotcha" from `CLAUDE.md`. Each file documents a specific, non-obvious architectural decision or trap in HuginnDB's codebase — file names, function names, issue references and all the original detail preserved.

This is internal documentation for AI coding agents (Claude Code and similar) and contributors doing fixes on this repo. It is deliberately kept **outside `docs/`**: `docs/*.md` is auto-indexed into the app's own in-app Documentation viewer (see [gotcha #50](gotcha-050-docs-viewer-markdown-derived-navigation.md) — `src/lib/appInfo/docOutline.ts` derives that viewer's navigation from the markdown files it finds there), and these ADRs are not meant to appear to end users.

Before touching code in an area with history (Zustand, Monaco, tab_state, MySQL/MongoDB decoding, dockview, environments, MCP, ...), check the index below and read the relevant ADR in full — the one-line summary is not a substitute.

| # | Gotcha | Resumen | Fecha |
| - | ------ | ------- | ----- |
| 1 | [Zustand selectors must be reference-stable](gotcha-001-zustand-selector-stability.md) | Selectors like `s => s.entries.filter(...)` return a fresh array or object on every call, so `Object.is` always sees a difference and React re-renders forever. Subscribe to raw state and derive arrays/objects with `useMemo` in the component instead. | 2026-09-03 |
| 2 | [Monaco is bundled, never CDN-loaded](gotcha-002-monaco-bundled-not-cdn.md) | `monaco-setup.ts` wires workers via Vite `?worker` imports and calls `loader.config({ monaco })`; without this the SQL and cell editors go blank whenever Tauri can't reach a CDN. Don't reintroduce a CDN dependency. | 2026-09-03 |
| 3 | [tauri::State does not auto-deref into &T](gotcha-003-tauri-state-inner-deref.md) | Call `state.inner()` to get `&AppState` at call sites. Every helper that takes `&AppState` is called with `state.inner()`, never `&state`. | 2026-09-03 |
| 4 | [quote_ident is for catalog identifiers, not user input](gotcha-004-quote-ident-catalog-only.md) | User input always goes through bound parameters (`$1` / `?`), never `quote_ident`, except for the one sanctioned, validation-mediated exception in the DDL builder documented separately (gotcha #16). | 2026-09-03 |
| 5 | [update_cell's value is Option<String> end-to-end](gotcha-005-update-cell-option-string.md) | The cell editor only ever emits text and drivers cast textual literals server-side, so `serde_json::Value` can't be pushed through `update_cell` — sqlx postgres won't encode `Value` to arbitrary column types. | 2026-09-03 |
| 6 | [MySQL order_clause no longer string-rewrites identifiers](gotcha-006-mysql-order-clause-quoting.md) | `fetch_table_data` computes `pg_or_sqlite` once and passes the right boolean straight to `quote_ident`, replacing the old brittle `"col"` → `` `col` `` string-rewriting hack. Don't reintroduce it. | 2026-09-03 |
| 7 | [DataGrid mutation callbacks use row values, not a row index](gotcha-007-datagrid-row-identity-not-index.md) | TanStack's `row.index` is the filtered display index while parents used to resolve the PK from the unfiltered backend page, so the two diverge as soon as a client filter is active. Anything needing identity must read `row.original`, the values array passed to the callback. | 2026-09-03 |
| 8 | [tab_state.json is v5 environments, main-window-only](gotcha-008-tab-state-environments-main-window-only.md) | `tab_state.json` holds a list of environments plus a global origins registry, and only the main window ever touches it — guarded by the single-spelling `isMainWindow()` check. Each environment owns launch state (`activeConnections`/`selectedConnectionId`/`activeTabId`) restored in a load-bearing order by `useEnvironments.restoreSession`. | 2026-09-03 |
| 9 | [Monaco commands are per-editor; providers are global to the language](gotcha-009-monaco-ctrl-enter-global-providers.md) | Ctrl+Enter must be bound via `editor.addCommand` inside `handleMount` through a ref (a `window` keydown listener never sees it), while the completion and CodeLens providers must be registered exactly once per Monaco instance via a per-model registry, not once per editor tab, or they duplicate N times with N open tabs. | 2026-09-03 |
| 10 | [The nested workspace dockview is synced from useTabs one way](gotcha-010-dockview-synced-to-usetabs.md) | `TabbedArea`'s inner dockview only flows store → dockview for add/remove. Split/float geometry is persisted once per environment and restored exactly once at launch, strictly after the launch reconnect has populated `useTabs` — running it earlier collapses the layout back to default. | 2026-09-03 |
| 11 | [sqlx rejects Vec<u8> reads on a MySQL BIT column](gotcha-011-mysql-bit-vecu8-rejected.md) | Its blob-compatibility check only accepts BLOB/STRING/VARBINARY. See gotcha #15 for the full, current MySQL integer/BIT decode-and-write story, which supersedes this note. | 2026-09-03 |
| 12 | [Double-click on a grid cell edits inline, not in a modal](gotcha-012-datagrid-inline-cell-edit.md) | `DataGrid.openCellEdit` routes by column type — a single-column FK opens `FkCombobox`, an editable cell opens inline `CellInput`, a read-only query result opens the modal as a viewer — tracked by the row's values-array identity rather than a display index. | 2026-09-03 |
| 13 | [Grid "zoom" is one persisted rowHeight preference](gotcha-013-grid-zoom-row-height-pref.md) | `DataGrid` derives cell height, padding and font size from `gridPrefs.rowHeight` and adjusts it via a non-passive native `Ctrl`+wheel listener, since a JSX `onWheel` is passive and can't `preventDefault` to suppress page zoom. It already round-trips through `prefs.json`. | 2026-09-03 |
| 14 | [An undeclared field on a typed IPC struct is silently dropped](gotcha-014-serde-drops-undeclared-fields.md) | `save_tab_state` deserializes into a strongly-typed struct with `#[serde(default)]` but no flatten catch-all, so a frontend-only field must be declared in both `src/types.ts` and the Rust struct or serde discards it before it ever reaches disk. | 2026-09-03 |
| 15 | [MySQL integer and BIT decoding is width- and type-specific](gotcha-015-mysql-integer-bit-decoding.md) | `mysql_value` tries signed-then-unsigned integer widths before falling back to NULL, must route sqlx's `"BOOLEAN"`-named `TINYINT(1)` columns into the integer branch explicitly, and writing a `BIT` cell needs `CAST(? AS UNSIGNED)` since a plain textual `"1"` would otherwise store the ASCII byte. | 2026-09-03 |
| 16 | [The structure editor builds DDL in Rust, never in the component](gotcha-016-structure-editor-ddl-in-rust.md) | `preview_structure_change` and `apply_structure_change` both call the same pure `build_ddl` builder so what's previewed is exactly what runs. User-entered identifiers go through `validate_ident`/`validate_type`/`validate_default` as the one sanctioned exception to the never-let-user-input-reach-`quote_ident` rule. | 2026-09-03 |
| 17 | [MySQL BLOB vs TEXT is decided by content, not the type name](gotcha-017-mysql-blob-text-content-sniffing.md) | The server sometimes flags genuine text columns as `BINARY` at the protocol level, so `mysql_value` reads raw bytes via `try_get::<Vec<u8>>` and validates UTF-8 itself. Using `try_get::<String>` first would reject a `BINARY`-flagged `LONGTEXT` before ever inspecting the bytes, collapsing it to hex. | 2026-09-03 |
| 18 | [SSH tunnel bind falls back to an ephemeral port on collision](gotcha-018-ssh-tunnel-port-fallback.md) | `open_tunnel` retries `bind(("127.0.0.1", 0))` on `AddrInUse` and points the pool at the actually-bound port rather than the profile's pinned one; the saved profile itself is never rewritten. | 2026-09-03 |
| 19 | [Building a WebviewWindow must happen inside an async Tauri command](gotcha-019-webview-window-builder-async-fn.md) | A sync command building a window on Windows deadlocks — a documented WebView2 issue — and renders a blank, "Not Responding" window. Only marking the command `async fn` actually fixes it, per Tauri's own docs and tauri#13963. | 2026-09-03 |
| 20 | [huginndb-mcp lives in its own workspace crate, never a second [[bin]]](gotcha-020-mcp-sidecar-separate-workspace-crate.md) | `tauri-bundler`'s Windows bundling enumerates every `[[bin]]` in the package's own `cargo metadata` regardless of `required-features` gating and fails sizing a binary a normal build never compiles, so the MCP binary was split into a sibling `mcp-server` crate instead. | 2026-09-03 |
| 21 | [Windows installer target is NSIS, not MSI/WiX](gotcha-021-windows-installer-nsis-not-msi.md) | WiX v3's `light.exe` reliably failed to launch on GitHub's Windows runners (an archived, unmaintained toolchain, likely flagged by AV heuristics); NSIS is Tauri's officially supported upgrade path from a prior MSI install, not the reverse. | 2026-09-03 |
| 22 | [huginndb-mcp ships as a Tauri externalBin sidecar staged by CI](gotcha-022-mcp-sidecar-external-bin-staging.md) | The release workflow builds the `mcp-server` crate and copies it to the exact `externalBin` naming convention before `tauri-action` runs. `tauri-build` hard-fails any compile of `huginndb_lib` without a placeholder sidecar file already on disk, which the workflow touches before building the real binary. | 2026-09-03 |
| 23 | [A running huginndb-mcp.exe blocks the self-updater's overwrite](gotcha-023-mcp-sidecar-blocks-updater.md) | Tauri's generated NSIS template only closes the main exe before updating, so a live MCP sidecar causes `ERROR_SHARING_VIOLATION` that surfaces as a generic permissions failure. An `NSIS_HOOK_PREINSTALL` installer hook force-kills the sidecar before any files are copied. | 2026-09-03 |
| 24 | [The updater polls periodically but only installs on an explicit click](gotcha-024-updater-periodic-checks-install-gating.md) | `startPeriodicChecks` re-runs the launch check every 4h and auto-downloads in the background, but `install()` — which overwrites files, force-kills the MCP sidecar, and can prompt elevation — only ever runs from an explicit user click, and must be called on the exact `Update` instance `download()` ran on. | 2026-09-03 |
| 25 | [An untargeted frontend listener receives every window's emit_to](gotcha-025-tauri-listen-target-scoping.md) | Tauri delivers an event to any listener whose registered target is `EventTarget::Any`, so `startLogBridge` must pass `{ target: getCurrentWindow().label }` or a second window's Console duplicates the first window's SQL/connection log entries. | 2026-09-03 |
| 26 | [Canary isolates on-disk state but deliberately shares the keychain](gotcha-026-canary-state-isolation-shared-keychain.md) | `app_identity::APP_DIR` flips to `"HuginnDB-Canary"` under the `canary` feature so every state file lands in a separate config dir, while the keychain service stays build-unaware on purpose so canary can dogfood real profiles. Never hardcode `"HuginnDB"` for a new state file. | 2026-09-03 |
| 27 | [An environment scopes session state, not which connections exist](gotcha-027-environment-scopes-session-not-connections.md) | `tab_state.json` v4+ holds a list of `Environment`s, each owning its own `connections` map, layout and launch state, resolved via `active_environment_mut()`. This is a distinct identity axis, deliberately not a revival of the deleted v2 "workspaces." | 2026-09-03 |
| 28 | [components/ is grouped by domain with an optional dialogs/ subfolder](gotcha-028-components-domain-grouped-dialogs.md) | Each domain folder keeps its tabs/trees/panels at the root and files single-purpose modals under `dialogs/`; `common/` is reserved for genuinely cross-domain, store-free leaves. Several 1000+ line "god files" were split one-file-per-level, with non-JSX logic extracted into `lib/` hooks. | 2026-09-03 |
| 29 | [The Mongo list view edits using row_types, not the display JSON's types](gotcha-029-document-list-view-row-types.md) | `bson_to_json` is lossy on purpose, so `docs_to_result` ships a parallel `row_types` array that every write sends back as `update_cell`'s `column_type`. Fields are addressed by dotted path, and opaque types (`Binary`, `MinKey`, …) are not inline-editable. | 2026-09-03 |
| 30 | [All per-engine SQL text goes through db::sql::Dialect](gotcha-030-dialect-single-sql-builder.md) | `Dialect::try_of` is the single conversion point and MongoDB is refused as `UnsupportedDriver`; a `_ =>` arm in any dialect match is a smell — new engines have silently inherited Postgres's behavior through exactly that pattern before. | 2026-09-03 |
| 31 | [SQL Server needs its own pool (tiberius) and four T-SQL divergences](gotcha-031-mssql-tiberius-pool-and-tsql.md) | `MsSqlPool` wraps a semaphore-guarded stack of `tiberius` clients that only return to the pool on a `Server`-classified error; pagination needs an injected `ORDER BY`, inserts prefer `OUTPUT INSERTED` with a `SCOPE_IDENTITY()` fallback for triggers, and binary writes need an explicit `CONVERT` — DDL editing, renames and `.sql` export/import stay deliberately deferred. | 2026-09-03 |
| 32 | [The command palette's index lives in lib/; settings navigation is a string contract](gotcha-032-command-palette-index-separate-from-component.md) | `CommandPalette.tsx` is presentation-only; entries, mode prefixes and fuzzy scoring live in `lib/commandPalette/`. An entry's `id` doubles as its MRU key, and `SETTINGS_INDEX[].prefId` must exactly match the `PrefRow` prop it targets. | 2026-09-03 |
| 33 | [A MongoDB view is a stored pipeline, so its editor is the aggregation editor](gotcha-033-mongo-view-is-aggregation-pipeline.md) | The frontend never parses a pipeline — stage bodies cross IPC as source text parsed by `shell::parse_relaxed_value` — and reading a saved view back must use `bson_to_shell_text`, not the lossy display converter, or re-saving unchanged can silently break matching. | 2026-09-03 |
| 34 | [MongoDB indexes are read from raw listIndexes, never mongodb::IndexModel](gotcha-034-mongo-indexes-raw-not-indexmodel.md) | The typed `IndexModel` discards key direction, index type, and most options, so there are deliberately two index readers — a lossy one feeding the SQL-shaped explorer DTO and a full-fidelity one feeding the index manager, which also refuses editing the `_id_` index and treats "edit" as parse-validate-then-drop-and-create. | 2026-09-03 |
| 35 | [An environment's export is cosmetics + connections + origins, never session state](gotcha-035-environment-export-portable-identity.md) | `export_environments`/`import_environment` round-trip a dedicated file format that excludes tabs, layout and launch state as machine-local artifacts, dedupes shared connection profiles across bundled environments, and import always mints fresh environment ids rather than merging. | 2026-09-03 |
| 36 | [Schema refresh must target the child slice, not the parent connection id](gotcha-036-schema-refresh-multi-db-scoping.md) | A server-wide connection's tables live under synthetic `<parent>::db::<db>` child slices, so `refreshTree` fans out to every opened child; refresh must also invalidate cached columns/indexes/errors, not just replace the databases/tables lists. | 2026-09-03 |
| 37 | [SQL Server accepts SSMS's HOST\INSTANCE in either field, split centrally](gotcha-037-mssql-host-instance-splitting.md) | `split_instance` in `db/mssql/mod.rs` is the single authoritative normalization, run before the tunnel branch is opened; `lib/db/driver.ts`'s `splitSqlServerName` is only a UI-side preview twin. | 2026-09-03 |
| 38 | [MongoDB can rename a collection but genuinely cannot rename a view](gotcha-038-mongo-rename-collection-not-view.md) | `rename_table`'s Mongo arm uses the `admin`-database `renameCollection` run-command with `dropTarget` false, refuses views up front, and closes (rather than retitles) tabs on a cross-database move since the destination is a different connection id. | 2026-09-03 |
| 39 | [JSON Schema bindings are global metadata resolved by a Rust-only cascade](gotcha-039-json-schema-binding-cascade-global.md) | The schema library and its column bindings live in a dedicated global `json_schemas.json`; bodies are opaque source text never parsed in Rust, resolution specificity is column > table > db_schema > connection, and validation never gates a save. | 2026-09-03 |
| 40 | [Shared origins are a global registry, not per-environment state](gotcha-040-shared-origins-global-registry.md) | `tab_state.json` v5 moved origins out of `Environment` into a top-level, deduped-by-path registry, for the same reason JSON Schema bindings are global (gotcha #39). Vanished-origin detection has both a proactive notice and a durable reconciliation sweep as a fallback. | 2026-09-03 |
| 41 | [Every JSON state file's path, load and atomic save go through state_file.rs](gotcha-041-state-file-single-io-module.md) | `load_or_default` silently degrades for preferences and known-hosts, while `tab_state` and `json_schemas` propagate parse errors for their own reasons, and `store::load_profiles` deliberately uses neither since an empty profile list is never a safe default. | 2026-09-03 |
| 42 | [db::exec is the one place SQL actually runs, not scattered pool matches](gotcha-042-db-exec-single-execution-layer.md) | `db::exec` exposes `execute`/`execute_params`/`scalar_i64`/`query_rows`/`execute_all`/`in_tx_expect_at_most_one` over bind-loop macros, replacing duplicated `match`-over-`DbPool` dispatch that used to live independently in several `commands/` files. | 2026-09-03 |
| 43 | [Catalog SQL lives in db/<driver>/schema.rs; DTOs stay with the command](gotcha-043-catalog-sql-driver-owned-dtos-command-owned.md) | `commands/schema.rs` is now dispatch-only with no `unreachable!()` arms; DTOs stay next to the commands that serialize them as the IPC contract, and `strip_view_header` lives in `db/view_ddl.rs` since that's the module that builds view headers in the first place. | 2026-09-03 |
| 44 | [useAsyncSubmit deliberately keeps submitting true after success](gotcha-044-use-async-submit-stays-true-on-success.md) | Every success closes or replaces the dialog, so clearing the flag first would re-enable buttons for the frames before unmount — a double-submit window on a destructive action. It shares one flag and error slot across a dialog's two actions. | 2026-09-03 |
| 45 | [Three frontend seams that must keep their intentional divergence](gotcha-045-intentional-shared-code-divergences.md) | `useListNavigation`'s `wrap` flag and `useUi`'s `LaunchView` `undefined`-vs-`null` distinction were extracted with their divergence on purpose — collapsing either for "consistency" is a regression, and per-feature debounce constants are kept separate for the same reason. | 2026-09-03 |
| 46 | [The three import dialogs share useImportWizard with one default conflict action](gotcha-046-import-wizard-shared-skip-default.md) | Profiles, environments and JSON Schemas all run pick → passphrase → conflicts → done over `lib/transfer/useImportWizard.ts`; the shared `"skip"` default (previously an inconsistent `"rename"` vs `"skip"`) is what stops re-importing your own export from accumulating renamed duplicates. | 2026-09-03 |
| 47 | [PrefId is derived from Preferences, making gotcha #32's contract a compile error](gotcha-047-prefid-compile-time-derived.md) | `lib/prefId.ts` builds the settings-navigation string union from the `Preferences` type plus `ActionId` for shortcut rows, so a `prefId` typo that used to silently fail to highlight now fails to compile. | 2026-09-03 |
| 48 | [drop_view must verify the name is actually a view before dropping it](gotcha-048-mongo-drop-view-type-check.md) | A view and a collection share one namespace, so `view_presence` resolves `Absent`/`Collection`/`View` in one `listCollections` round trip and `drop_view` refuses anything but a confirmed view — critical once a caller, like the MCP tool, can pass a merely-guessed name. | 2026-09-03 |
| 49 | [Three MCP write-tool wiring steps aren't compiler-enforced, unlike the rest](gotcha-049-mcp-write-tool-checklist-unenforced-arms.md) | `bridged_connection_id`, `policy_id_of` and `class_of` all end in `_ => None`, so a forgotten write variant silently skips the app-side policy re-check or the audit `conn=` field rather than failing to build — and can let a read-only connection perform a write. | 2026-09-03 |
| 50 | [The docs viewer slices navigation from the markdown itself, in one scan](gotcha-050-docs-viewer-markdown-derived-navigation.md) | `docOutline.ts`'s `parseDoc` produces the outline and page bodies in a single pass so two scans can't disagree about section boundaries, skips fenced code so shell comments aren't mistaken for headings, and `slugify` follows GitHub's punctuation-dropping rule to match existing anchors. | 2026-09-03 |
| 51 | [Markdown's navigator is React context, and its scroll-to-anchor clears last](gotcha-051-markdown-navigator-context-scroll-order.md) | `renderBlocks` is memoized on `source` alone, so a callback prop would either go stale or force a full reparse; `DocsDialog`'s `ScrollToAnchor` clears the pending anchor inside the `requestAnimationFrame` after scrolling, not before, or the effect's own cleanup cancels the frame. | 2026-09-03 |
| 52 | [cargo test writes to the real state directory unless a seam prevents it](gotcha-052-cargo-test-real-state-directory-risk.md) | `state_file::path` has no test override, so a test reaching `save_profiles`/`state_file::save_atomic` overwrites the developer's real `profiles.json`. The fix is extracting a testable seam (a pure `merge_into` taking `&mut Vec` plus live-pool ids) rather than mocking. | 2026-09-03 |
| 53 | [A keystroke resolves in exactly one place; scope comes from the DOM](gotcha-053-keybinding-dispatch-single-resolver-dom-scope.md) | `resolveBindings` returns a fresh `Map` so it can never be a Zustand selector; `data-kb-scope` on containers plus `scopesAt(activeElement)` decides applicability, `ActionSpec.fixed` declares non-rebindable bindings, and Monaco keeps its own `onKeyDown` feeding the same dispatcher for consistency. | 2026-09-03 |
| 54 | [A statement's write tier is classified from its text in exactly one place](gotcha-054-statement-classify-text-based-single-source.md) | `db/classify.rs`'s `MongoOp::class()` is an exhaustive match so a new mongosh operation without a stated tier is a build error — fixing a real privilege-escalation bug where the old SQL-keyword heuristic tiered every Mongo read as `DataWrite` because the pool wasn't available to ask. | 2026-09-03 |
| 55 | [The schema tree filter never opens connections and has one owner](gotcha-055-schema-tree-search-single-owner-no-prefetch.md) | Typing only searches what's already loaded in `useSchema` (warming further is an explicit button); `treeMatches.ts` distinguishes pending/unloaded/none/matches/out-of-scope instead of a misleading `0`, and `useTreeSearch` is deliberately its own ephemeral, non-persisted store. | 2026-09-03 |
| 56 | [The shared-origin editor edits a document with three load-bearing rules](gotcha-056-origin-doc-editor-merge-rules.md) | `origin_doc/` is a pure model touching no local state, which is what makes its merge rules testable; an unchanged secret is re-encrypted only via cached identity fields staying sacred, and `publish_impact` runs the real `merge_into` to surface any silently-dropped consumers. | 2026-09-03 |
| 57 | [The Mongo completion provider uses a cursor scanner, not a parser](gotcha-057-mongo-pipeline-completion-context-scanner.md) | `monacoMongo.ts` follows `monacoSql.ts`'s per-model registry pattern to avoid duplicate providers, and `completionContext.ts`'s forward scan answers "what key/stage is the cursor in" without validating or building an AST, staying consistent with gotcha #33's one-parser rule. | 2026-09-03 |
| 58 | [MCP exposure lives on the profile; --connections has three distinct states](gotcha-058-mcp-exposed-set-profile-tri-state-flag.md) | `ConnectionProfile::mcp_exposed` replaces a client-config-only allowlist and is re-read per call rather than cached from the handshake, so it can change mid-session; `Config.pinned`'s `None`/`Some(set)`/`Some(empty)` states are all load-bearing. | 2026-09-03 |
| 59 | [MCP tool annotations are read once at startup, so they must be constants](gotcha-059-mcp-tool-annotation-constants-split.md) | Deriving `readOnlyHint` from live policy would leave a stale, unsafe-looking hint cached by the client for the session, so `run_query` and `run_write` are two separate tools instead of one dynamically-annotated one. | 2026-09-03 |
| 60 | [components/ui/ has a strict dependency rule, one density scale, three focus styles](gotcha-060-ui-library-dependency-rule-density-focus.md) | `ui/` may only import `react`/`@radix-ui/*`/`lucide-react`/`cva`/`@/lib/utils` — nothing stateful — with density as `size` (`xs`/`sm`/`md`), `brand` (not `primary`) as the action color, and three named focus treatments in `ui/styles.ts`. | 2026-09-03 |
| 61 | [Feedback and transition state follow a written doctrine in CONTRIBUTING.md](gotcha-061-feedback-transition-state-doctrine.md) | Four rules — name the subject of an async transition, destructive-on-hover with separate confirmation tiers, busy belongs to the control via `loading`, and confirm writes only when the effect isn't self-evident — are meant to be applied at a shared seam rather than re-derived per call site. | 2026-09-03 |
